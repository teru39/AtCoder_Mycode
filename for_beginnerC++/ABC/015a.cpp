#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
  string s, ss;
  cin >> s >> ss;
  if (s.length() > ss.length())
    cout << s << endl;
  else
    cout << ss << endl;
}
